this database runs an ai invoice pipeline. it tracks users, handles file uploads with duplicate detection, 
and stores ocr text with spatial data. it extracts names, dates, and amounts using nlp, logs every processing step,
 and keeps an audit trail of human corrections.
 Note====================
 these are the initial database and not yet the final
